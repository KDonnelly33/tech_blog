
<article>
    <div>
        <h2>{{blog.title}}</h2>
       
        <div>
            Posted by {{blog.user.username}} on {{format_date blog.date_created}}
        </div>
    </div>
    <div>{{blog.content}}</div>
</article>
 
{{!-- if post has comments post comments --}}
{{#if blog.comments}}
    {{#each blog.comments}}
            <div> 
                <h3>{{comment}} </h3>
                <div>Posted by {{user.username}} on {{format_date date_created}}
                    
                </div>

    


            </div>
    {{/each}}
{{!-- if post has no comments post no comments --}}
{{else}}
    <div>No comments yet</div>
{{/if}}

<form id = "newcommentForm">

    <div>
        <h3>Add New Comment</h3>
    </div>

    <div>
        <input type="hidden" name="blog-id" value="{{blog.id}}" />
        <label for="comment-body">Comment</label>
        <textarea id = "#new-body" name="comment-body"></textarea>
        <button type="submit">Submit</button>
    </div>
</form>

<script src="/js/comment.js"></script>
```